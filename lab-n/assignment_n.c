// Includes
#include <stdio.h>
#include <string.h>

// Constants
#define LEN 4

// Main function
int main(void) {

  // Read user input
  char user_input[LEN];
  printf("Enter a domain including the . (e.g. .uk): ");
  fgets(user_input, LEN, stdin);

  // Enter your code in this section
  // Use strcmp(user_input, country_codes[0].domain) to compare the text in
  // user_input to the text in country_codes[0].domain.

  // Do not edit below here
  return 0;
}
